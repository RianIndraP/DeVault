{
  "name": "devault",
  "version": "1.0.0",
  "description": "DeVault - aplikasi penyimpan kata sandi (belajar)",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./styles/input.css -o ./styles/output.css --minify",
    "watch": "tailwindcss -i ./styles/input.css -o ./styles/output.css --watch"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
